server:
  port: 8080

spring:
  application:
    name: egov-project
  datasource:
    url: jdbc:h2:mem:egovdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# JWT Configuration Placeholder
jwt:
  secret: "v3ryS3cr3tK3yF0rJwTS1gn1ngPurp0s3sOnlYDoN0tUs3InPr0duct10n"
  access-token-validity-in-seconds: 3600
  refresh-token-validity-in-seconds: 86400

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.example.egov: DEBUG
